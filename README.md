### chatGPT with cTransformers

install the ctransformers module (once only) by:
```
pip install ctransformers
```

run it with (pull the pre-trained model file chat.gguf in the same directory):
```
python chat.py
```
